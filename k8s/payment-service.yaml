apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: techlab-ecommerce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
      - name: payment-service
        image: techlab-payment-service:latest
        ports:
        - containerPort: 3002
        envFrom:
        - configMapRef:
            name: techlab-config
        - secretRef:
            name: techlab-secrets
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: techlab-ecommerce
spec:
  selector:
    app: payment-service
  ports:
  - port: 3002
    targetPort: 3002
